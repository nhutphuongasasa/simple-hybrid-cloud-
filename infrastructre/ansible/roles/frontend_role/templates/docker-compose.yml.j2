version: '3.8'
services:
  frontend:
    image: {{ frontend_image }}
    ports:
      - "{{ frontend_port }}:80"
    restart: always

  nginx:
    image: {{ nginx_image }}
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - frontend